

export type collectionList = {
    id: number,
    title: string,
    numberOfBooks: number,
    urlCover: string
}

export type collectionBooks = {
    id: number,
    title: string,
    year: string,
    urlCover: string
}

export type statisticsList = {
    label: string,
    value: string
}